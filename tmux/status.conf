# claude-tmux status-line segment — needs-input badge counter.
# Appends a segment to status-right showing how many sessions need input.
# To use: `source-file ~/git/claude-tmux/tmux/status.conf` in ~/.tmux.conf
# AFTER your existing status-right configuration.
set -agq status-right ' #(cct-status-segment) '
